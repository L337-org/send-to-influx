-r requirements.txt
-e .
black~=26.5.1
flake8~=7.3.0
flake8-bugbear~=25.11.29
flake8-black~=0.4.0
# Docstring rules (pydocstyle). Installing this turns D on by default, which is why the
# convention, the decorator exemption and the backlog ignore-list all land in tox.ini in
# the same change - without them this pin alone would fail CI on the whole existing backlog.
flake8-docstrings~=1.7.0
# Docstring/signature agreement (pydoclint). pydocstyle only checks the sections a docstring
# chose to write, so a function that documents no parameters at all passes it entirely. This is
# the check that closes that, and it registers as a flake8 plugin, so it runs in the job that
# already gates.
pydoclint~=0.9.1
pytest~=9.1.1
pytest-cov~=7.1.0
mypy~=2.3.1
# For the SPDX licence-list check in tests/test_repo_hygiene.py. pytest already pulls
# packaging in, but only at >=22, and packaging.licenses arrived in 24.2 - so a floor
# that actually has the module is declared here rather than inherited by luck.
packaging~=26.3.0
# The process harness serves its stub Hue bridge over TLS, because a real bridge is HTTPS
# with a self-signed certificate and pointing the collector's own handler at plain HTTP
# would exercise a path that does not exist. The certificate is generated per run rather
# than committed - a private key in a fixture is a private key in the repository, whatever
# it is for - and this is the only way to generate one without shelling out to openssl.
# abi3 wheels, so one build serves every supported minor: verified with
# `pip download cryptography~=48.0.1 --only-binary=:all: --python-version 310` and 314,
# which resolve to cp39-abi3 and cp311-abi3 manylinux wheels.
cryptography~=48.0.1
types-PyYAML~=6.0.12
types-requests~=2.33.0
